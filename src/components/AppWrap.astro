---
import { cn } from '@/lib/utils';

interface Props {
  id: string;
  className?: string;
}

const { id, className } = Astro.props;
---

<div id={id} class={cn('relative', className)}>
  <slot />
</div>
