---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Navbar from '@/components/Navbar.astro';
import Footer from '@/components/Footer.astro';
import ProjectCard from '@/components/ProjectCard.astro';
import FilterStrip from '@/components/FilterStrip.astro';
import { getAllProjects } from '@/lib/content';

const projects = await getAllProjects();
const allTags = [...new Set(projects.flatMap((p) => p.data.exif.tags))].sort();
---

<BaseLayout
  title="Work — case studies"
  description={`Case studies · ${projects.length} project${projects.length === 1 ? '' : 's'}`}
>
  <Navbar />

  <main
    class="
      px-4 pt-4 pb-6 md:px-6 md:pt-6 md:pb-7 lg:px-9 lg:pt-9 lg:pb-7
      max-w-[1320px] mx-auto
    "
  >
    <p
      class="text-[11px] text-mute-2 tracking-[0.18em] uppercase mb-2 font-mono"
    >
      <a href="/" class="hover:text-phosphor transition-colors">// home</a>
      &nbsp;/&nbsp;work
    </p>

    <h1
      class="m-0 font-display font-bold text-paper-hi text-[clamp(32px,5vw,56px)] tracking-[-0.02em] leading-[0.95] mb-2"
    >
      Work
    </h1>

    <p class="text-[12px] text-mute-2 tracking-[0.14em] font-mono mb-8">
      // case studies · <span data-filter-count>{projects.length}</span> entr{projects.length === 1 ? 'y' : 'ies'}
    </p>

    <FilterStrip tags={allTags} />

    {projects.length === 0 ? (
      <p class="text-[12px] text-mute-2 font-mono">// no projects yet</p>
    ) : (
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3" data-filter-target>
        {projects.map((p, i) => (
          <div data-tags={p.data.exif.tags.join('|')}>
            <ProjectCard project={p} index={i} total={projects.length} />
          </div>
        ))}
      </div>
    )}
  </main>

  <Footer />
</BaseLayout>
